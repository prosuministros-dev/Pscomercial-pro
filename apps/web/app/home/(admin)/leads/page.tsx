import { LeadsPageClient } from './_components/leads-page-client';

export const metadata = {
  title: 'Leads | Pscomercial',
  description: 'Gestión de leads comerciales',
};

export default function LeadsPage() {
  return <LeadsPageClient />;
}
